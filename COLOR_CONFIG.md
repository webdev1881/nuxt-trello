# Налаштування кольорів проекту

## Primary колір

Primary колір використовується для всіх основних елементів інтерфейсу: кнопок, посилань, акцентів тощо.

## Розташування конфігурації

### 1. Tailwind конфігурація (`tailwind.config.ts`)

Тут визначається палітра кольору `primary` з відтінками від 50 до 950:

```typescript
export default <Partial<Config>>{
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#eff6ff',   // Найсвітліший
          100: '#dbeafe',
          200: '#bfdbfe',
          300: '#93c5fd',
          400: '#60a5fa',
          500: '#2072fd',  // Основний колір (це ваш #2072fd)
          600: '#1d66e0',
          700: '#1a56c3',
          800: '#1747a6',
          900: '#143789',
          950: '#0f2a5c',  // Найтемніший
        },
      },
    },
  },
}
```

### 2. App конфігурація (`app.config.ts`)

Тут вказується яку палітру використовувати як primary:

```typescript
export default defineAppConfig({
  ui: {
    primary: "primary",  // 👈 Використовує палітру з tailwind.config.ts
    gray: "neutral",
  },
});
```

## Як змінити колір

### Варіант 1: Використати стандартний Tailwind колір

**Файл**: `app.config.ts`

```typescript
export default defineAppConfig({
  ui: {
    primary: "blue",    // або red, green, purple, pink, yellow тощо
    gray: "neutral",
  },
});
```

**Доступні кольори**:
- `slate`, `gray`, `zinc`, `neutral`, `stone`
- `red`, `orange`, `amber`, `yellow`, `lime`, `green`, `emerald`, `teal`, `cyan`, `sky`, `blue`, `indigo`, `violet`, `purple`, `fuchsia`, `pink`, `rose`

### Варіант 2: Використати кастомний колір (як зараз)

**Крок 1**: Створіть/змініть `tailwind.config.ts` з палітрою кольорів

**Крок 2**: Змініть значення `500` (основний відтінок) на ваш hex код:

```typescript
primary: {
  500: '#YOUR_COLOR_HERE',  // Ваш колір
}
```

**Крок 3**: Згенеруйте інші відтінки (50-950) за допомогою інструментів:
- https://uicolors.app/create
- https://www.tailwindshades.com/

**Крок 4**: Вкажіть в `app.config.ts`:

```typescript
primary: "primary",
```

**Крок 5**: Перезапустіть сервер:

```bash
# Ctrl+C для зупинки
npm run dev
```

## Приклад: Змінити на зелений

### Швидкий спосіб:

**`app.config.ts`**:
```typescript
export default defineAppConfig({
  ui: {
    primary: "green",  // 👈 Просто змініть на green
    gray: "neutral",
  },
});
```

### Кастомний відтінок зеленого:

**`tailwind.config.ts`**:
```typescript
export default <Partial<Config>>{
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#f0fdf4',
          100: '#dcfce7',
          200: '#bbf7d0',
          300: '#86efac',
          400: '#4ade80',
          500: '#22c55e',  // 👈 Основний зелений
          600: '#16a34a',
          700: '#15803d',
          800: '#166534',
          900: '#14532d',
          950: '#052e16',
        },
      },
    },
  },
}
```

**`app.config.ts`**:
```typescript
primary: "primary",
```

## Поточна конфігурація

- **Основний колір**: `#2072fd` (синій)
- **Палітра**: Кастомна (визначена в `tailwind.config.ts`)
- **Використання**: `primary: "primary"` в `app.config.ts`

## Важливо

⚠️ Після зміни `tailwind.config.ts` потрібно **перезапустити сервер** (не просто HMR):

```bash
# Зупинити (Ctrl+C) та запустити знову
npm run dev
```

## Інструменти для генерації палітри

1. **UI Colors**: https://uicolors.app/create
   - Введіть ваш hex код
   - Автоматично згенерує всі відтінки 50-950

2. **Tailwind Shades**: https://www.tailwindshades.com/
   - Простий інтерфейс
   - Експорт готової конфігурації

3. **Coolors**: https://coolors.co/
   - Генератор палітр
   - Підбір гармонійних кольорів
